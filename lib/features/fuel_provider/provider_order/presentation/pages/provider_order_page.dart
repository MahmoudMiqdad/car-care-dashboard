import 'package:flutter/material.dart';

class ProviderOrderPage extends StatelessWidget {

  const ProviderOrderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ProviderOrder Page'),
      ),
    );
  }

}
