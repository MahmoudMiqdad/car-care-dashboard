import 'package:flutter/material.dart';

class UserFuelOrdersPage extends StatelessWidget {

  const UserFuelOrdersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('UserFuelOrders Page'),
      ),
    );
  }

}
